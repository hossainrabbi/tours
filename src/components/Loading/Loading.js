import React from 'react';
import './Loading.css';

const Loading = () => {
    return (
        <div className="Loading">
            <h2>Loading...</h2>
        </div>
    );
};

export default Loading;
